import { ActiveMsgCntDirective } from './active-msg-cnt.directive';

describe('ActiveMsgCntDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveMsgCntDirective();
    expect(directive).toBeTruthy();
  });
});
