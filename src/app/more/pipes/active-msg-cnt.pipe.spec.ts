import { ActiveMsgCntPipe } from './active-msg-cnt.pipe';

describe('ActiveMsgCntPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveMsgCntPipe();
    expect(pipe).toBeTruthy();
  });
});
